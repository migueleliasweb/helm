apiVersion: v1
kind: Service
metadata:
  name: {{ include "dep2.fullname" . }}
  labels:
    {{- include "dep2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dep2.selectorLabels" . | nindent 4 }}
